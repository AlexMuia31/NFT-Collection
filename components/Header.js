import { AppBar, Box, Toolbar } from "@mui/material";
import React from "react";

const Header = () => {
  return (
    <Box>
      <AppBar>
        <Toolbar></Toolbar>
      </AppBar>
    </Box>
  );
};

export default Header;
